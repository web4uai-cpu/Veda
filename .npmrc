# Don't publicly hoist @types/* — web uses React 19 types while mobile
# (Expo) uses React 18 types; hoisting either to the root breaks the
# other app's typecheck. (pnpm's default pattern includes *types*.)
public-hoist-pattern[]=*eslint*
public-hoist-pattern[]=*prettier*
